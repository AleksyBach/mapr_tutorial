 - hosts: all
   tasks:
    # - name: Create staff Group
    #   group: name=staff gid=20 state=present
    - name: Create chufe User
      user: name=chufe uid=501 password=maprpassword createhome=yes state=present
